"""Sets = conjuntos 
aqui em Python e os conjuntos são muito similares as listas e aos dicionários, ou 
seja, um conjunto é uma coleção de elementos que você pode adicionar dentro de uma 
mesma estrutura de dados. Porém a maior diferença entre os sets e as listas ou as 
tuplas por exemplo é que o sets só suportam elementos únicos e não possuem índices e
não segue ordem (fica tudo misturado dentro do conjunto)."""

# *.add* (adiciona), *.update* (atualiza), *.clear*, *.discard* (remove)
# union *|* (une),
# intersection *&* (todos os elementos presentes nos dois sets)
# diferrence *-* (elementos apenas no set da esquerda)
# symmetric_difference *^* (elementos que estão nos dois sets, mas não em ambos)


